{
  "private": true,
  "type": "module",
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "cli": "clear; npm -w cli run",
    "start": "npm run cli -- start",
    "stage": "npm run cli -- stage",
    "build": "npm run cli -- build"
  },
  "dependencies": {
    "@awesome.me/buildawesome": "^4.0.0-0",
    "@buildawesome.one/cli": "^1.4.0"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
