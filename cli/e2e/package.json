{
  "private": true,
  "name": "cli.e2e",
  "type": "module",
  "scripts": {
    "cli": "clear; BA_OPTIONS=--input=cli/e2e/ npm -C ../ run",
    "start": "npm run cli -- start",
    "stage": "npm run cli -- stage",
    "build": "npm run cli -- build"
  }
}
